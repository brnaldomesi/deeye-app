import { Colors, Size } from 'src/styles';

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  divider: {
    marginVertical: Size(0.7),
    backgroundColor: Colors.divider
  }
});

export default styles;
